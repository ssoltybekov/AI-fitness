# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0   # ASGI server to run FastAPI

# Database
sqlalchemy==2.0.30          # ORM — maps Python classes to DB tables
asyncpg==0.29.0             # async PostgreSQL driver (needed for SQLAlchemy async)
alembic==1.13.1             # DB migrations (version control for schema changes)

# Computer Vision
mediapipe==0.10.14          # pose landmarks
opencv-python==4.9.0.80     # video reading/frame processing
numpy==1.26.4               # array math for angle calculation

# Vector DB / RAG
chromadb==0.5.0             # local vector store

# LLM client
httpx==0.27.0               # async HTTP — to call Ollama REST API

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6      # async test support
httpx                       # also used for TestClient in FastAPI tests